package pqvqntort.glyphs.model;
import java.util.*;

public class Curl {

    Set<Subsegment> cavity;
    Set<Subsegment> arm;

    public Curl() {
        cavity = new HashSet<>();
        arm = new HashSet<>();
    }

}
